<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Background color -->
            <corners android:radius="8dp" /> <!-- Radius defines the roundness of corners -->
        </shape>
    </item>

    <!-- Border -->
    <item android:right="1dp" android:left="1dp" android:top="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <stroke android:width="1dp" android:color="#E0E0E0" /> <!-- Border color and width -->
            <corners android:radius="8dp" /> <!-- Radius should match the background -->
        </shape>
    </item>
</layer-list>
